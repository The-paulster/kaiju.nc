// Role: expose KAIJU Reconstructor formatting options. Keep formatting rules in
// formatter.js and command-palette UI in command.js.
const { getFormattingOptions } = require("./formatter");

module.exports = {
	getFormattingOptions
};
